version: '3.8'

services:
  # --------------------------------
  # 1. Jenkins CI/CD Server Container
  # --------------------------------
  jenkins:
    image: jenkins/jenkins:lts
    # Run as root to prevent permission issues with accessing mounted binaries/socket
    user: root 
    privileged: true 
    ports:
      - "8080:8080"
      - "50000:50000"
    volumes:
      - jenkins_home:/var/jenkins_home
      # CRITICAL DOCKER MOUNTS:
      - /var/run/docker.sock:/var/run/docker.sock
      # Mount host binaries (check both common paths if one fails)
      - /usr/bin/docker:/usr/bin/docker
      - /usr/bin/docker-compose:/usr/bin/docker-compose
      # If the above docker-compose mount fails, try this one (comment out the above one if you test this)
      # - /usr/local/bin/docker-compose:/usr/local/bin/docker-compose
    environment:
      TZ: Asia/Kolkata
    restart: always
    networks:
      - portfolio_network

  # --------------------------------
  # 2. Application Container (Node)
  # --------------------------------
  app:
    image: syed048/portfolio-app:latest
    env_file:
      - .env
    ports:
      - "80:3000"
    depends_on:
      - db
    restart: always
    networks:
      - portfolio_network

  # --------------------------------
  # 3. MySQL Database Container
  # --------------------------------
  db:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: 123admin
      MYSQL_DATABASE: portfolio_db
      MYSQL_USER: syed
      MYSQL_PASSWORD: 123admin
    volumes:
      - db_data:/var/lib/mysql
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    restart: always
    networks:
      - portfolio_network

  # --------------------------------
  # 4. Adminer (DB UI) Container
  # --------------------------------
  adminer:
    image: adminer
    restart: always
    ports:
      - "8081:8080"
    networks:
      - portfolio_network

networks:
  portfolio_network:
    driver: bridge

volumes:
  jenkins_home:
  db_data:
